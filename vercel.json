{
  "version": 2,
  "cleanUrls": true,
  "installCommand": "pnpm install --frozen-lockfile",
  "functions": {
    "api/index.ts": {
      "maxDuration": 60
    }
  },
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "/api/index?route=api&path=:path*"
    },
    {
      "source": "/.well-known/:path*",
      "destination": "/api/index?route=well-known&path=:path*"
    },
    {
      "source": "/health",
      "destination": "/api/index?route=health"
    }
  ],
  "buildCommand": "pnpm --filter frontend build"
}
