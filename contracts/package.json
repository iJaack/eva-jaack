{
  "name": "contracts",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "${FOUNDRY_BIN:-$HOME/.foundry/bin/forge} build",
    "test": "${FOUNDRY_BIN:-$HOME/.foundry/bin/forge} test",
    "fmt": "${FOUNDRY_BIN:-$HOME/.foundry/bin/forge} fmt"
  }
}
